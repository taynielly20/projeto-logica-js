/**Crie uma variável chamada "numero" e atribua a ela qualquer número inteiro. 
 * Verifique se o número é par ou ímpar utilizando uma estrutura condicional 
 * (if-else) e exiba a mensagem no console. */

let numero = 13;

if (numero % 2 === 0) {
  console.log("O número é par.");
} else {
  console.log("O número é ímpar.");
}
