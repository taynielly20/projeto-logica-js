/** Crie uma variável chamada "contador" e atribua a ela o valor 0. Em seguida, utilize um loop (for) para exibir no console os números de 1 a 5,
 *  incrementando o valor do contador a cada iteração.*/

let contador = 0;

for (let i = 1; i <= 5; i++) {
  contador += 1;
  console.log(contador);
}
